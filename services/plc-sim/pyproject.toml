[project]
name = "plc-sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tharittapol", email = "tharitpol.big@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pymodbus>=3.6,<4.0",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
