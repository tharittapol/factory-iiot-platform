[project]
name = "plc-sim"
version = "0.1.0"
description = "Modbus TCP simulator for an industrial drying chamber PLC"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymodbus>=3.15,<4.0",
    "pyyaml>=6.0",
]

[project.scripts]
plc-sim = "plc_sim.main:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/plc_sim"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
